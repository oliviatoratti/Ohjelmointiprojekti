W a) Why does an arrival event need to generate a new arrival event?
- So that new customers continue entering the system during the simulation

b) Which event should be generated by the start of the service (of a single service point)?
- The start of service should generate an exit event